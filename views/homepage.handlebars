<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="./">Home</a>
        </li>
        {{#if logged_in}}
        <li class="nav-item">
          <a class="nav-link" href="/profile">Profile</a>
        </li>
        {{/if}}
        {{#if logged_in}}
        <li class="nav-item">
          <a class="nav-link" id='logout' href="#">Log Out</a>
        </li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>

<div class="section">
  <div class="container">
    <div class="content">
      <h1 class="title profile-title">{{ title }}</h1>
    </div>

    <!-- start of owner section -->
    <div class="content">
      <div class="card">
        <div class="card-content">
          {{!-- <h2 class="subtitle">Your Info</h2> --}}

          <div class="columns">
  
            <!-- name column -->
            <div class="column">
              <form>
                <div class="field is-horizontal">
                  <div class="field-body">
                    <div class="field">
                      <div class="control" id="nameControl"></div>
                    </div>
                    <div class="field">
                      <div class="control">
                      </div>
                    </div>
                  </div>
                </div>
                <div class="control">
                  <div class="dropdown">
                    <div class="dropdown-trigger">
                      {{!-- <button type="submit" class="btn btn-primary">Your Events</button> --}}
        
                        <span class="icon is-small">
                          <i class="fas fa-angle-down" aria-hidden="true"></i>
                        </span>
                      </button>
                    </div>
                    <div class="dropdown-menu" id="dropdown-menu2" role="menu">
                      <div class="dropdown-content" id="userEventsDropdown">
                        {{!-- dropdown goes here --}}
                      </div>
                    </div>
                  </div>
                </div>
              </form>
          </div>
        </div>
      </div>
    </div>
<!-- Loop over each object in the projects array -->
<div class="list-group" >
{{#each events as |event| }}
<a href="/events/{{event.id}}" class="list-group-item list-group-item-action" aria-current="true">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1">{{event.event_name}}</h5>
      <small>{{format_date event.createdAt}}</small>
    </div>
    <p class="mb-1">{{event.description}}</p>
    <small>{{format_amount event.total_cost}}</small>
  </a>
{{/each}}
</div>


{{!-- <!-- Loop over each object in the projects array -->
{{#each events as |event| }}

<div id='events' class="list-group">
  <a href="#" class="list-group-item list-group-item-action active" aria-current="true">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1">List group item heading</h5>
      <small>3 days ago</small>
    </div>
    <p class="mb-1">Some placeholder content in a paragraph.</p>
    <small>And some small print.</small>
  </a>
  <a href="#" class="list-group-item list-group-item-action">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1">List group item heading</h5>
      <small class="text-muted">3 days ago</small>
    </div>
    <p class="mb-1">Some placeholder content in a paragraph.</p>
    <small class="text-muted">And some muted small print.</small>
  </a>
  <a href="#" class="list-group-item list-group-item-action">
    <div class="d-flex w-100 justify-content-between">
      <h5 class="mb-1">List group item heading</h5>
      <small class="text-muted">3 days ago</small>
    </div>
    <p class="mb-1">Some placeholder content in a paragraph.</p>
    <small class="text-muted">And some muted small print.</small>
  </a>
</div>
{{/each}} --}}

{{!-- <!-- Loop over each object in the projects array -->
{{#each events as |event| }}

<div class="row mb-4 project">
  <div class="col-md-5">
    <h2>
      <a href="/eventst/{{events.id}}">{{events.name}}</a>
    </h2>
    <p>
      <span class="dollar-amount">{{format_amount event.total_cost}}</span>
      needed!
    </p>
    <p>Created by {{events.user.name}} on {{format_date event.date_created}}</p>
  </div>
  <div class="col-md-7">
    <p>
      {{events.description}}
    </p>
  </div>
</div>
{{/each}} --}}
